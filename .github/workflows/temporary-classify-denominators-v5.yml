name: temporary-classify-denominators-v5

on:
  push:
    branches:
      - feat/plan-v3-gap-closure-2026-08-29

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: temporary-classify-denominators-v5
  cancel-in-progress: false

jobs:
  materialize-classify-and-verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Fetch exact branch and enough history for materializer recovery
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$GITHUB_WORKSPACE"
          git init "$GITHUB_WORKSPACE"
          git -C "$GITHUB_WORKSPACE" remote add origin \
            "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git -C "$GITHUB_WORKSPACE" fetch --no-tags --depth=500 origin "$GITHUB_REF_NAME"
          git -C "$GITHUB_WORKSPACE" checkout -B "$GITHUB_REF_NAME" FETCH_HEAD
          test "$(git -C "$GITHUB_WORKSPACE" rev-parse HEAD)" = "$GITHUB_SHA"
          git -C "$GITHUB_WORKSPACE" show -s --format='commit=%H%ntree=%T'

      - name: Recover and execute repository-native materializer when required
        env:
          GH_TOKEN: ${{ github.token }}
          CANDIDATE_REPOSITORY: ${{ github.repository }}
          CANDIDATE_REF: ${{ github.ref_name }}
          CANDIDATE_SHA: ${{ github.sha }}
        shell: bash
        run: |
          set -euo pipefail
          if test -d manifests/upstream/candidates/raw \
            && test -f docs/status/DENOMINATOR_MATERIALIZATION.json \
            && test -f manifests/upstream/denominator-review-packet.draft.json; then
            echo 'raw denominator candidates already materialized'
            exit 0
          fi
          path=.github/workflows/temporary-denominator-materialization-v4.yml
          commit=$(git log --all --format=%H -- "$path" | head -n1)
          test -n "$commit"
          git show "$commit:$path" >/tmp/materializer.yml
          python3 - <<'PY'
          from pathlib import Path
          lines = Path('/tmp/materializer.yml').read_text(encoding='utf-8').splitlines()
          start = next(index + 1 for index, line in enumerate(lines) if "python3 - <<'PY'" in line)
          content = []
          for line in lines[start:]:
              if line.strip() == 'PY':
                  break
              content.append(line[10:] if line.startswith('          ') else line)
          Path('/tmp/materialize.py').write_text('\n'.join(content) + '\n', encoding='utf-8')
          PY
          export GITHUB_TOKEN="$GH_TOKEN"
          export GITHUB_REPOSITORY="$CANDIDATE_REPOSITORY"
          export GITHUB_REF_NAME="$CANDIDATE_REF"
          export GITHUB_SHA="$CANDIDATE_SHA"
          export GITHUB_RUN_ID
          export GITHUB_RUN_ATTEMPT
          python3 /tmp/materialize.py

      - name: Classify all fourteen denominator candidates
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/classify-materialized-denominators.py
          python3 scripts/classify-materialized-denominators.py --check
          python3 scripts/check-denominator-materialization.py --require-present
          python3 scripts/check-denominator-materialization.py --require-review-ready

      - name: Validate complete source corpus
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/check-plan.py
          python3 -m unittest discover -s tests -p 'test_*.py' -v
          git diff --check

      - name: Commit verified non-workflow review packet
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "trillionnium-gap-closure-bot"
          git config user.email "gap-closure-bot@users.noreply.github.com"
          git add \
            manifests/upstream/candidates \
            manifests/upstream/denominator-review-packet.draft.json \
            docs/status/DENOMINATOR_MATERIALIZATION.json \
            docs/review/DENOMINATOR_REVIEW_PACKET.md
          if git diff --cached --quiet; then
            echo 'exact review packet already current'
          else
            git commit -m "feat(sg1): materialize and classify exact denominator set"
            git push origin "HEAD:${GITHUB_REF_NAME}"
          fi
          printf 'head=%s\ntree=%s\n' \
            "$(git rev-parse HEAD)" "$(git rev-parse HEAD^{tree})"

      - name: Refresh independent reviewer request
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          curl -fsS -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'Accept: application/vnd.github+json' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/pulls/42/requested_reviewers" \
            -d '{"reviewers":["ProfHepta"]}' >/dev/null || true

name: denominator-materialization-gate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/check-denominator-materialization.py'
      - 'tests/control_plane/test_denominator_materialization.py'
      - 'docs/status/DENOMINATOR_MATERIALIZATION.json'
      - 'docs/review/DENOMINATOR_REVIEW_PACKET.md'
      - 'manifests/upstream/candidates/**'
      - 'manifests/upstream/denominator-review-packet.draft.json'
      - '.github/workflows/denominator-materialization-gate.yml'
  push:
    branches: [main]
    paths:
      - 'scripts/check-denominator-materialization.py'
      - 'tests/control_plane/test_denominator_materialization.py'
      - 'docs/status/DENOMINATOR_MATERIALIZATION.json'
      - 'docs/review/DENOMINATOR_REVIEW_PACKET.md'
      - 'manifests/upstream/candidates/**'
      - 'manifests/upstream/denominator-review-packet.draft.json'
      - '.github/workflows/denominator-materialization-gate.yml'

permissions:
  contents: read

concurrency:
  group: denominator-materialization-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CANDIDATE_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
  CANDIDATE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}

jobs:
  fail-closed-materialization:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Fetch exact candidate without external actions
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$GITHUB_WORKSPACE"
          git init "$GITHUB_WORKSPACE"
          git -C "$GITHUB_WORKSPACE" remote add origin \
            "https://github.com/${CANDIDATE_REPOSITORY}.git"
          git -C "$GITHUB_WORKSPACE" fetch --no-tags --depth=1 origin \
            "${CANDIDATE_SHA}"
          git -C "$GITHUB_WORKSPACE" checkout --detach FETCH_HEAD
          test "$(git -C "$GITHUB_WORKSPACE" rev-parse HEAD)" = "$CANDIDATE_SHA"
          git -C "$GITHUB_WORKSPACE" show -s --format='commit=%H%ntree=%T'
      - name: Validate candidates, digests, counts and claim boundary
        run: python3 scripts/check-denominator-materialization.py
      - name: Run materialization regression tests
        run: python3 -m unittest tests.control_plane.test_denominator_materialization -v
      - name: Prove draft packet cannot grant SG1
        shell: bash
        run: |
          set -euo pipefail
          if python3 scripts/check-denominator-materialization.py --require-review-ready; then
            echo 'materialization is ready for independent review; SG1 still remains external-review gated'
          else
            echo 'materialization remains incomplete and grants no SG1 credit'
          fi

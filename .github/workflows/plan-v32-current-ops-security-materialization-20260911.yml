name: plan-v32-current-ops-security-materialization-20260911

on:
  push:
    branches:
      - ops/game-exact-source-snapshot-20260910
    paths:
      - .github/workflows/plan-v32-current-ops-security-materialization-20260911.yml
      - tools/materialize_plan_v32_ops_security_current.py
  workflow_dispatch:

permissions:
  checks: read
  contents: write
  issues: write
  pull-requests: write

concurrency:
  group: plan-v32-current-ops-security-materialization-20260911
  cancel-in-progress: false

jobs:
  materialize-current-main:
    runs-on: ubuntu-24.04
    timeout-minutes: 240
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Fetch exact materializer source without external actions
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$GITHUB_WORKSPACE"
          git init "$GITHUB_WORKSPACE"
          git -C "$GITHUB_WORKSPACE" remote add origin \
            "https://github.com/${GITHUB_REPOSITORY}.git"
          git -C "$GITHUB_WORKSPACE" fetch --no-tags --depth=1 origin "$GITHUB_SHA"
          git -C "$GITHUB_WORKSPACE" checkout --detach FETCH_HEAD
          test "$(git -C "$GITHUB_WORKSPACE" rev-parse HEAD)" = "$GITHUB_SHA"
          test -s "$GITHUB_WORKSPACE/tools/materialize_plan_v32_ops_security_current.py"
          test -s "$GITHUB_WORKSPACE/tools/materialize_plan_v32_closure.py"

      - name: Install exact Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.85.1 \
            --profile minimal \
            --component rustfmt \
            --component clippy
          printf 'RUSTUP_TOOLCHAIN=1.85.1\n' >> "$GITHUB_ENV"

      - name: Materialize and verify current-main candidate
        shell: bash
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          python3 -m py_compile tools/materialize_plan_v32_ops_security_current.py
          python3 tools/materialize_plan_v32_ops_security_current.py

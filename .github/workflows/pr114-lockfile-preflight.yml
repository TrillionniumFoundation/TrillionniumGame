name: pr114-lockfile-preflight

on:
  push:
    branches:
      - tooling/server-composition-preflight-2026-09-06
    paths:
      - .github/workflows/pr114-lockfile-preflight.yml

permissions:
  contents: write

concurrency:
  group: pr114-lockfile-preflight
  cancel-in-progress: false

env:
  TARGET_BRANCH: codex/server-composition-root-v2-2026-09-06
  TARGET_HEAD: 9ef64a8b93b52808bbbde0acaf74c382e6b4b15f

jobs:
  preflight:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - name: Fetch exact target branch
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$GITHUB_WORKSPACE"
          git init "$GITHUB_WORKSPACE"
          git -C "$GITHUB_WORKSPACE" remote add origin \
            "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          observed="$(git -C "$GITHUB_WORKSPACE" ls-remote origin \
            "refs/heads/${TARGET_BRANCH}" | awk '{print $1}')"
          test "$observed" = "$TARGET_HEAD"
          git -C "$GITHUB_WORKSPACE" fetch --no-tags --depth=1 origin "$TARGET_HEAD"
          git -C "$GITHUB_WORKSPACE" checkout --detach FETCH_HEAD

      - name: Generate exact isolated lock and execute crate
        shell: bash
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          rustup toolchain install 1.85.1 --profile minimal --component rustfmt --component clippy
          rustup override set 1.85.1
          rm -f crates/trnm-server/Cargo.lock
          cargo generate-lockfile --manifest-path crates/trnm-server/Cargo.toml
          cargo fmt --manifest-path crates/trnm-server/Cargo.toml -- --check
          cargo test --manifest-path crates/trnm-server/Cargo.toml --all-targets --locked
          cargo clippy --manifest-path crates/trnm-server/Cargo.toml \
            --all-targets --locked -- -D warnings
          git diff --check
          actual="$(git status --short | awk '{print $2}' | sort | paste -sd, -)"
          test "$actual" = "crates/trnm-server/Cargo.lock"

      - name: Commit exact lock to unchanged target branch
        shell: bash
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          git config user.name trillionnium-server-bot
          git config user.email trillionnium-server-bot@users.noreply.github.com
          git add crates/trnm-server/Cargo.lock
          git commit -m "build(server): lock canonical composition dependencies"
          repaired="$(git rev-parse HEAD)"
          observed="$(git ls-remote origin "refs/heads/${TARGET_BRANCH}" | awk '{print $1}')"
          test "$observed" = "$TARGET_HEAD"
          git push origin "${repaired}:refs/heads/${TARGET_BRANCH}"

name: plan-v32-remaining-work-sync-20260909

on:
  push:
    branches:
      - codex/plan-v32-controller-v2-2026-09-09
    paths:
      - .github/workflows/plan-v32-remaining-work-sync-20260909.yml
      - tools/sync_plan_v32_remaining_work.sh
      - docs/internal/plan-v32-remaining-work.request

permissions:
  contents: write
  issues: write
  pull-requests: read

concurrency:
  group: plan-v32-remaining-work-sync-20260909
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Fetch exact controller branch
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$GITHUB_WORKSPACE/controller"
          git init "$GITHUB_WORKSPACE/controller"
          git -C "$GITHUB_WORKSPACE/controller" remote add origin \
            "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git -C "$GITHUB_WORKSPACE/controller" fetch --no-tags origin "$GITHUB_SHA"
          git -C "$GITHUB_WORKSPACE/controller" checkout -B codex/plan-v32-controller-v2-2026-09-09 FETCH_HEAD
          test "$(git -C "$GITHUB_WORKSPACE/controller" rev-parse HEAD)" = "$GITHUB_SHA"
      - name: Synchronize every open gap and denominator family
        env:
          GH_TOKEN: ${{ github.token }}
          ROOT: ${{ github.workspace }}/controller
        shell: bash
        run: bash "$GITHUB_WORKSPACE/controller/tools/sync_plan_v32_remaining_work.sh"

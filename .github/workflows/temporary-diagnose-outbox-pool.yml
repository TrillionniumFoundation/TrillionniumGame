name: temporary-diagnose-outbox-pool

on:
  push:
    branches:
      - codex/outbox-spool-worker-2026-08-31

permissions:
  contents: read

concurrency:
  group: temporary-diagnose-outbox-pool
  cancel-in-progress: true

jobs:
  diagnose-postgresql:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Fetch exact branch without external actions
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$GITHUB_WORKSPACE"
          git init "$GITHUB_WORKSPACE"
          git -C "$GITHUB_WORKSPACE" remote add origin \
            "https://github.com/${GITHUB_REPOSITORY}.git"
          git -C "$GITHUB_WORKSPACE" fetch --no-tags --depth=1 origin "$GITHUB_SHA"
          git -C "$GITHUB_WORKSPACE" checkout --detach FETCH_HEAD
          test "$(git -C "$GITHUB_WORKSPACE" rev-parse HEAD)" = "$GITHUB_SHA"
      - name: Expose only stable domain reason in diagnostic build
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          path = Path('crates/trnm-persistence-pg/src/bin/trnm-outbox-worker.rs')
          source = path.read_text(encoding='utf-8')
          old = '''                "domain_failure(code={},retry={:?})",
                          error.code().as_str(),
                          error.retry(),
          '''
          new = '''                "domain_failure(code={},reason={},retry={:?})",
                          error.code().as_str(),
                          error.reason(),
                          error.retry(),
          '''
          if source.count(old) != 1:
              raise SystemExit(f'expected one diagnostic anchor, found {source.count(old)}')
          path.write_text(source.replace(old, new, 1), encoding='utf-8')
          PY
          rustup toolchain install 1.85.1 --profile minimal
          rustup override set 1.85.1
      - name: Load immutable PostgreSQL image
        shell: bash
        run: |
          set -euo pipefail
          TRNM_POSTGRES_IMAGE=$(python3 - <<'PY'
          import json
          from pathlib import Path
          value = json.loads(Path('config/database-test-images.json').read_text())
          print(value['profiles']['postgresql']['image'])
          PY
          )
          echo "TRNM_POSTGRES_IMAGE=$TRNM_POSTGRES_IMAGE" >>"$GITHUB_ENV"
      - name: Reproduce exact pool failure with stable reason
        env:
          TRNM_EVIDENCE_ROOT: run/outbox-pool-diagnostic
          TRNM_RUN_ID: ${{ github.run_id }}-${{ github.run_attempt }}
        shell: bash
        run: bash scripts/ci-outbox-spool-worker.sh postgresql

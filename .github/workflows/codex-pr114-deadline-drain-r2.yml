name: codex-pr114-deadline-drain-r2

on:
  push:
    branches:
      - codex/server-composition-root-v2-2026-09-06

permissions:
  contents: write

jobs:
  repair:
    if: github.repository == 'TrillionniumFoundation/TrillionniumGame'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Fetch exact repair head
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_BRANCH: codex/server-composition-root-v2-2026-09-06
        run: |
          set -euo pipefail
          basic="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git init "$GITHUB_WORKSPACE"
          git -C "$GITHUB_WORKSPACE" remote add origin "https://github.com/$GITHUB_REPOSITORY.git"
          git -C "$GITHUB_WORKSPACE" -c http.https://github.com/.extraheader="AUTHORIZATION: basic $basic" fetch --no-tags --depth=1 origin "$GITHUB_SHA"
          git -C "$GITHUB_WORKSPACE" checkout -B "$TARGET_BRANCH" FETCH_HEAD
          test "$(git -C "$GITHUB_WORKSPACE" rev-parse HEAD)" = "$GITHUB_SHA"

      - name: Apply deterministic source repair
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/codex-pr114-deadline-drain-r2.py

      - name: Install exact Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.85.1 --profile minimal --component rustfmt --component clippy
          rustup override set 1.85.1

      - name: Format test and lint server workspace
        shell: bash
        run: |
          set -euo pipefail
          cargo fmt --manifest-path crates/trnm-server/Cargo.toml
          cargo test --manifest-path crates/trnm-server/Cargo.toml --all-targets --locked
          cargo clippy --manifest-path crates/trnm-server/Cargo.toml --all-targets --locked -- -D warnings

      - name: Remove repair-only tooling and publish source commit
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_BRANCH: codex/server-composition-root-v2-2026-09-06
        run: |
          set -euo pipefail
          rm -f scripts/codex-pr114-deadline-drain.py
          rm -f scripts/codex-pr114-deadline-drain-r2.py
          rm -f scripts/codex-pr114-trigger.txt
          rm -f scripts/codex-pr114-trigger-r2.txt
          rm -f .github/workflows/codex-pr114-deadline-drain.yml
          rm -f .github/workflows/codex-pr114-deadline-drain-r2.yml
          git config user.name "ProfHepta"
          git config user.email "102159240+ProfHepta@users.noreply.github.com"
          git add -A
          test -n "$(git status --porcelain)"
          git commit -m "fix(server): enforce total ingress deadlines and active drain"
          basic="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.https://github.com/.extraheader="AUTHORIZATION: basic $basic" push origin "HEAD:refs/heads/$TARGET_BRANCH"

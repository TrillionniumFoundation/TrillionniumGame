[package]
name = "trnm-token-jwt-provider-adapter"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
description = "Provider-backed strict HS256 JWT authentication boundary"
publish = false

[dependencies]
trnm-token-crypto-provider = { path = "../trnm-token-crypto-provider" }
trnm-token-jwt-adapter = { path = "../trnm-token-jwt-adapter" }

[lib]
path = "src/lib.rs"

[workspace]
